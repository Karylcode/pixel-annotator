# 交接文件 — 像素畫工具（pixel-annotator）

寫於 2026-08-17 深夜。給接手的 AI / 工程師：讀完這份就能繼續，不必翻對話紀錄。

---

## 1. 專案在哪、長什麼樣

- 根目錄：`C:\Users\Karyl\Desktop\Pixelart\AI Pixel Assets`
- **純靜態、無建置、無框架、無 npm**：`index.html` 用 `<script>` 依序載入 `js/codec.js → pixelate.js → store.js → render.js → ui.js → main.js`，全部掛在 `window.PA` 命名空間。直接用瀏覽器開 `index.html` 就能跑。
- 本機預覽：`.claude/launch.json` 的 `static`（`python -m http.server 8765`）→ `http://localhost:8765/index.html`。
- GitHub：https://github.com/Karylcode/pixel-annotator（public，`main`）。本機已 `git init`，`origin/main` 追蹤中。
- `.gitignore` 排除：`.claude/`、`_舊版前端備份/`、`代碼審查報告.md`。`.gitattributes`：`* text=auto eol=lf`。

```
index.html            主頁（標題列 / 左欄工具 / 畫布 / 右欄分頁 / 裁切列 / 縮圖列 / 行動版 dock / 三個對話框）
css/style.css         全部樣式（Design Token 在 :root；≤1280 緊湊、≤900 行動版抽屜、≤560 手機）
js/codec.js           純資料轉換：JSON/JS 解析、hex/rgba、點陣圖建構、PNG 放大、SVG、zip、RLE 網格 pack/unpack
js/pixelate.js        純運算：格線偵測（同步 + async）、重取樣、去背、OKLab k-means 減色
js/store.js           應用狀態 + 復原堆疊 + 存檔（localStorage）；不碰 DOM
js/render.js          把 store 畫到 canvas；不改狀態、不綁事件
js/ui.js              所有 DOM 與事件；要改資料一律呼叫 store，要重畫一律呼叫 render
js/main.js            兩行：PA.ui.init()
README.md             使用者文件（含 v2 已實作清單、行動版、驗收表）
UIUX修改設計文件.md    設計規格 v2.0（§6 細節、§7/§8 驗收）
代碼審查報告.md        2026-08-17 全面審查（§0 = 修復狀態、§2–4 = P0/P1/P2 清單、§6 = 開源比對）
_舊版前端備份/         v1 前端整份備份（不要動、不在 git 內）
```

**分層鐵律**（原作者訂的，請維持）：css = 外觀，ui.js = DOM/事件，render.js = 繪圖，store.js = 狀態。ui 不直接改 `store.state.hist` 之類的內部結構，要走 store 的函式。

---

## 2. 現在的 git 狀態（重要）

**有一批大改動已寫入磁碟、尚未 commit**：`git status` 會看到 `css/style.css index.html js/codec.js js/pixelate.js js/render.js js/store.js js/ui.js README.md` 為 modified（約 +1,150 / −380 行）。內容 = 審查報告 §0 列的全部 P0/P1 修復 + 兩個小功能（見 §4）。

- 這批改動**已通過**：`node --check` 六檔、瀏覽器功能測試（詳見 §6）、桌面版面指紋逐位相同、行動版煙霧測試、主控台零錯誤。
- **尚未通過**：對抗性審查代理只跑到一半就因額度停掉，沒有第二雙眼睛看過。接手者若要 commit，建議先自己讀一遍 `git diff`（重點是 `js/store.js` 復原模型與存檔、`js/ui.js` 的 bindCanvas / finishStroke / save_）。

commit 訊息建議（可直接用）：

```
修復審查報告 P0/P1：復原改稀疏 diff + 位元組預算、存檔 debounce + pixann.v2、手勢與筆畫分離、增量統計、無障礙

- store：4 種快照（grid / diff / rgba / pdiff）、192MB 預算、beginStroke/beginPixelStroke/endStroke/cancelLastAction、
  a.counts/annTotal/annOpaque 增量、im.rev 版本驅動所有快取、selectionMask 快取、floodFrom Int32 堆疊、
  pixann.v2（RLE 網格、savedAt、sel、stale 標記、v1 遷移、storage 事件）
- ui：gesture(crop/axis) 與 drawing 分離、finishStroke、save_ debounce + pagehide/beforeunload、rAF 合併重繪、
  色票 oninput/onchange 分離、Tab 只在無焦點時收合、inert、aria-selected + roving tabindex、關圖確認不先切圖
- render：drawParts Path2D 併矩形、mirrorAxisOf 記憶、thumb 64px 快取
- codec：顯示 canvas 不帶 willReadFrequently、bitmapFromRgba 不往返、尺寸守衛、packGrid/unpackGrid、jsIdent、zip 檔名清理
- pixelate：quantize 去掉 Math.max 展開、typed array + 收斂提前結束
- css/html：淺色主題 -fg 文字色、body.interacting 關 blur、safe-area 移到最底列、#stage 可聚焦
- 調色盤 >48 色時挑 OKLab 距離最遠的 48 色；像素化預設顏色上限 24→48
```

commit + push：

```bash
git add -A && git commit -F <訊息檔> && git push
```

---

## 3. 這批改動建立的關鍵不變量（改任何東西前先讀）

### 3.1 點陣圖版本 `im.rev`
每張圖 `state.imgs[i]` 有 `rev`（整數）。**任何改到像素或尺寸的路徑都必須 `im.rev++`**（`touchBitmap(im)`）：`paintPixel`、`applyPixelDiff`、`applyCrop`、`replaceImage`、`applyEntry`(rgba 分支)。所有快取都以 rev 為 key，rev 沒動快取就不會失效：
- store：`im._url`（存檔用 dataURL）、`im._pal`（調色盤）、`im._opaque`（不透明像素數）、`im._ctx`（canvas 2d context；換 cvs 時設 null）
- render：`im._thumb`（64px 縮圖）、`mirrorAxisOf` 的 `_axisIm/_axisRev`、`_sel`（魔棒遮罩）
- ui：`pxProfiles`（像素化邊緣能量，key = 圖物件 + rev）

### 3.2 復原堆疊（`state.hist[name] = {past, future}`）
四種 entry 形狀，每筆都帶 `{parts, next, active, shown, label}`：
| 形狀 | 欄位 | 誰建的 | 用途 |
|---|---|---|---|
| 整份網格 | `grid: Int16Array` | `snapshot(label)` | 魔棒 / 同色全選 / 清空 / 刪部件 / 匯入標註 |
| 標註稀疏 diff | `diff: {idx: Int32Array, val: Int16Array, n}` | `beginStroke(label)` | 筆刷 / 擦除筆畫（`setPx` 邊畫邊錄舊值，同一格只錄第一次） |
| 整張點陣圖 | `w, h, rgba, cvs, grid` | `snapshotImage(label)` | 裁切 / 像素化（存參照，因為舊物件會整個被換掉） |
| 像素稀疏 diff | `pdiff: {idx: Int32Array, rgba: Uint8ClampedArray, n}` | `beginPixelStroke(label)` | 繪圖 / 擦成透明（`paintPixel` 邊畫邊錄） |

- 錄製狀態 `rec`：`beginStroke/beginPixelStroke` 開始，`endStroke()` 結束（ui 在 pointerup 呼叫；`pushUndo` / `undoOnce` / `redoOnce` / `select` / `closeCurrent` / `applyCrop` / `replaceImage` 都會先隱式 `endStroke`）。共用遮罩 `_mask` 只在錄製結束時清掉動過的格。
- `undoOnce/redoOnce` 用 `counterpartOf(e)` 建「同形狀」的反向 entry 推到另一邊；`cancelLastAction()` = 撤掉最近一筆但不進 future（取消筆畫用）。
- 記憶體：`HIST_BUDGET = 192MB`（past + future 合計，估算值：grid.byteLength、diff/pdiff 實際長度、rgba×2）+ `UNDO_DEPTH = 60`。超過就整筆丟（先丟最遠 future，再丟最舊 past，最近一筆 past 永遠留）。**不再有「把舊快照瘦身成沒有 rgba 的形狀」這種邏輯，不要加回去。**
- `applyEntry` 對不上尺寸會回 `null` → 呼叫端截斷歷史。

### 3.3 標註統計（增量，不掃圖）
`a = state.ann[name]` 上：`counts{id:n}`、`annTotal`、`annOpaque`（有標註且像素 alpha ≥128）。`setPx` / `deletePart` / `clearGrid` / `applyEntry(diff)` 增量維護；整份換掉的路徑（`applyEntry` grid/rgba/pdiff、`applyCrop`、`replaceImage`、`applyAnnotation`、`addDecoded`、`restore`）呼叫 `recount(a, im)`。`coverage(im?)` / `partCounts()` / `hasAnnotation()` 都是 O(部件數)。**新增任何改 grid 的路徑都要維護這三個值。**

### 3.4 存檔（localStorage + IndexedDB）
- 鍵 `pixann.v2`：`{v:2, savedAt, imgs:[{name,w,h,idb?,url?}], ann:{name:{parts,next,grid:'rle1:…'}}, sel, cur}`。網格用 `codec.packGrid`（RLE + base64）；`unpackGrid(str, expectedLen)` 長度不符回 null。
- 點陣圖進 IndexedDB（庫 `pixann`、store `bitmaps`，鍵 = 檔名，值 `{w,h,rgba}`）。`im._idbRev === im.rev` 時 payload 只寫 `idb:1`，不夾 dataURL。IDB 失敗或尚未寫入則仍夾 `url`。
- 舊鍵 `pixann`（v1，`grid` 是純陣列）：只在沒有 `pixann.v2` 時讀一次遷移，**不寫、不刪**（`_舊版前端備份/` 的舊版還在用它）。
- `store.save()` 同步寫 localStorage，回傳 `{ok, reason?: 'quota'|'foreign'|'error', bytes?}`。`store.persist()` 先把髒圖寫進 IDB 再 `save()`。配額失敗：保留上一份完整快照，另寫 `pixann.v2.stale = 時間戳`；`restore()` 回報 `info.stale`，UI 明講「還原的是較舊的版本」。
- 跨分頁：`storage` 事件收到比本分頁 `lastWrite` 新的 `savedAt` → `foreignWrite`，之後 `save()` 一律回 `foreign`，直到 `takeOver()`（UI toast 有「以這個分頁為準」）。
- ui 端：`save_()` 只是設 `savePending` + 800ms trailing debounce → `flushSave()`（async `persist`）；`pagehide` / `beforeunload` 走同步 `flushSaveSync()` → `save()`；`visibilitychange(hidden)` 走 async flush。`beforeunload` 在 `saveState !== 'ok'` 時攔一下。配額警告只跳一次（`quotaWarned`），狀態列常駐 ⚠。
- 「檔案 ▾ → 清除瀏覽器裡的保存資料…」呼叫 `store.clearSaved()`（刪 v2 + stale + IDB，不動 v1）。

### 3.5 畫布互動（ui.js `bindCanvas`）
- `drawing`（筆畫進行中，會塗色）與 `gesture`（`'crop'` / `'axis'`，不塗色）**分開**；canvas `pointermove` 在 `gesture` 時不進塗色路徑。`gestureEnd` 存著結束目前手勢的函式，`setTool()`、觸控第二指落下、視窗 blur 都會呼叫。
- `finishStroke()`（pointerup / 切工具 / Ctrl+Z 時）：關錄製、`renderAll()`、`save_()`；`abortStroke()`（pointercancel / 第二指）：`store.cancelLastAction()`。
- 重繪：`draw()` 同步；`scheduleDraw()` 用 rAF 合併（pointermove 用這個）；`schedulePanelCounts()` 筆畫中只更新部件格數文字。**注意：瀏覽器面板隱藏時 rAF 不會跑**（見 §6）。
- `body.interacting` 在筆畫 / 平移期間加上，CSS 用它關掉三層 `backdrop-filter`。

### 3.6 鍵盤 / 無障礙
- `Tab` 收合右欄只在「焦點在 body、或滑鼠點在 #stage 上（無 `:focus-visible`）」時生效，其餘交給瀏覽器導覽。`#stage` 有 `tabindex=0`。
- 分頁列 `role=tab` 有 `aria-selected` + roving tabindex（`setTab` 同步）+ ← → Home End；模式切換也同步 `aria-selected`。
- 無圖時 `.rail > .grp`（除了含 `#b-pixelate2` 的「影像」組）與 `aside` 設 `inert`；裁切鈕 `disabled`。
- 淺色主題：文字用 `--accent-fg / --warn-fg / --success-fg / --danger-fg / --info-fg / --focus-ink`，底色 / 色條沿用 `--accent` 等亮色。新增文字用途請用 `-fg`。

---

## 4. 額度用完前最後加的兩個小功能

1. `store.imagePalette(limit=48)`：圖片色數 ≤48 時全部列出（不變）；>48 時從最常見的 512 個候選裡挑 OKLab 距離最遠的 48 個（√次數加權）。理由：未減色的 AI 圖前 48 名全是差 1–2 階的灰。
2. 像素化預設顏色上限 24 → 48：`index.html` 的 `#px-k` / `#px-k-range` 初始值，以及 `ui.js` 的 `PX_K_DEFAULT = 48`（仍會被實際色數夾住）。

---

## 5. 還沒做的（依報告 §4，全是 P2；接手者可挑）

P0 / P1 / §4 P2 與邊角已在 2026-08-17～18 做完。點陣圖改 IndexedDB、復原歷史改可停留面板。

**刻意不做（報告 §6 / §7）**
- 不引入 fflate / UPNG.js / magic-wand-js（零依賴；zip 檔名清理 + 4GB/65535 上限、scanline 泛洪已內建）
- 不引框架、不先 TypeScript、不把 pixelate 換成 WASM

---

## 6. 怎麼驗證（一定要跑）

### 6.1 桌面版面零回歸（使用者明確要求，任何 CSS 改動後必跑）
指紋腳本在 `C:\Users\Karyl\AppData\Local\Temp\claude\C--Users-Karyl-Desktop-Pixelart-AI-Pixel-Assets\f2e78895-937f-4b4e-9d17-ad00c686d6a3\scratchpad\fingerprint.js`（若目錄不在了，邏輯是：對 header / .rail / aside / #stage(含四邊 padding) / #cv / #status / #strip / #cropbar / #hud / #drop / .card 取 `getBoundingClientRect` + display，加上 zoom / zval / body class / mode / touch-action / 作用中分頁與工具，六種狀態各 JSON 一行：空狀態 → 載入範例劍 → Tab 收合（再展開）→ 載入範例藥水（兩張圖，縮圖列出現）→ 裁切工具（再取消）→ 繪圖模式）。基準值在同目錄 `fp-baseline-1440.json` / `fp-baseline-1280.json`。改動前後在 **1440×900 與 1280×720** 各跑一次，六行必須逐位相同（rAF 換成 `setTimeout` 等待、`--dur` 設 0ms）。

### 6.2 功能煙霧測試（用 `PA.store` / `PA.ui` API + 派發 PointerEvent 就能做，不需要人手）
- 筆刷筆畫 → `hist.past.at(-1).diff` 存在、`n` = 改到的格數；undo/redo 後 grid 逐位相同；`a.counts/annTotal/annOpaque` 與全掃結果相同
- 魔棒 → 整份 `grid` 快照；繪圖模式筆畫 → `pdiff`，undo/redo 後 `im.rgba` 與 canvas 讀回的像素相同
- 裁切整張 = 不進歷史；縮小裁切 → `rgba` 快照，undo 尺寸 / cvs.width / counts 回來
- 開鏡射差異 → 在對稱軸虛線上 pointerdown / move / up → `im.rgba` 逐位元組不變、歷史長度不變
- `flushSave()` → `localStorage['pixann.v2']` 有 `v:2`、`grid` 以 `rle1:` 開頭、`unpackGrid` 往返相等；重載後圖片 / 部件 / 標註 / 作用中部件都回來。點陣圖在 IndexedDB `pixann` / `bitmaps`（payload 圖筆有 `idb:1` 或 fallback `url`）
- 寫一份 v1 格式到 `pixann`、刪掉 v2、重載 → 遷移成功、v2 被寫出、v1 仍在
- 猴子補丁 `Storage.prototype.setItem` 對 `pixann.v2` 丟 `QuotaExceededError` → `save()` 回 `{ok:false, reason:'quota'}`、`pixann.v2.stale` 出現、舊快照仍在；`restore()` 回 `info.stale`
- 派發 `StorageEvent('storage', {key:'pixann.v2', newValue: JSON(savedAt 較新)})` → `save()` 回 `foreign`、toast 出現；`takeOver()` 後恢復
- `PA.pixelate.quantize` 對 65k 相異色不丟 `RangeError`
- 行動版 375×812：dock 56 高、抽屜把手是 `<button>` 且能關、單指起筆後第二指落下 → past 回復、future 0

### 6.3 瀏覽器面板（Claude 內建 preview）的坑
- python `http.server` 會快取 JS：改完先對每個檔 `fetch(url, {cache:'reload'})` 再導向，否則測到舊碼
- 面板隱藏時「not compositing」：截圖失敗、**rAF 永遠不會跑**（scheduleDraw / schedulePanelCounts / pxRecomputeSoon 都不會執行；測狀態不要測畫面）、CSS transition 凍結（`--dur:0ms`）、`resize` / matchMedia `change` 不會觸發、`setTimeout` 會被節流到極慢（像素化的 async 偵測會卡在進度條，不是 bug）
- 面板的 localStorage 每次開啟都是空的；`javascript_tool` 呼叫大約在 reload 後 5–8 秒才落地，短命的 toast 早就消失了

---

## 7. 不要做的事

- 不要改回「筆畫存整份 grid」或「大圖時把舊快照瘦身」—— 那是 P0 #2 / #3 的來源
- 不要在 pointermove 裡呼叫 `renderParts()` / 全圖掃描；不要在 `oninput` 裡呼叫 `save_()` 以外的存檔
- 不要在顯示用的 canvas 加 `willReadFrequently`
- 不要讓 `_舊版前端備份/`、`.claude/`、`代碼審查報告.md` 進 git（`.gitignore` 已排除）
- 不要動 `pixann`（v1）鍵的內容
- 改 CSS 前後一定跑 §6.1 指紋；行動版規則只能放在 `@media (max-width: 900px)` 內

---

## 8. 額度回來後原對話可以直接接的事
1. 「commit + push」
2. 「重跑審查」：`Workflow({scriptPath: 'C:\Users\Karyl\.claude\projects\C--Users-Karyl-Desktop-Pixelart-AI-Pixel-Assets\f2e78895-937f-4b4e-9d17-ad00c686d6a3\workflows\scripts\p0p1-fix-review-wf_e7da9d61-d0c.js', resumeFromRunId: 'wf_e7da9d61-d0c'})`（已完成的代理走快取）
