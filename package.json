{
  "name": "pixel-annotator",
  "private": true,
  "description": "Pixel art restoration and part annotation — runtime is static with zero npm dependencies.",
  "scripts": {
    "lint": "eslint .",
    "check": "node tools/check.js",
    "test:e2e": "playwright test",
    "test": "npm run lint && npm run check && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "eslint": "^9.33.0",
    "globals": "^16.3.0"
  }
}
